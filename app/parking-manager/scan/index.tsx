import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const EntryTransactions = () => {
	return (
		<View>
			<Text>EntryTransactions</Text>
		</View>
	);
};

export default EntryTransactions;

const styles = StyleSheet.create({});
