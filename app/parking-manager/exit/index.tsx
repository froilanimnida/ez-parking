import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ExitTransaction = () => {
	return (
		<View>
			<Text>ExitTransaction</Text>
		</View>
	);
};

export default ExitTransaction;

const styles = StyleSheet.create({});
