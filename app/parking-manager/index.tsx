import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ParkingManagerDashboard = () => {
	return (
		<View>
			<Text>ParkingManagerDashboard</Text>
		</View>
	);
};

export default ParkingManagerDashboard;

const styles = StyleSheet.create({});
